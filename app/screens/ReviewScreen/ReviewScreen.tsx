import React from 'react';
import {Text} from '../../components/Text';
import {type ScreenProps} from '../../navigators/AppNavigator';

export const ReviewScreen = (_props: ScreenProps<'Review'>) => {
  return <Text text="Review Screen" />;
};
